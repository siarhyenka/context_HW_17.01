export type Task = {
    userId: string;
    id: string;
    title: string;
    completed: boolean;
}

export type TaskDto = {
    userId: number;
    id: number;
    title: string;
    completed: boolean;
}